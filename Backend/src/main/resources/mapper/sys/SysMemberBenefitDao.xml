<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 会员私教权益后台查询与批量管理(第15步)。sys 目录 XML 热刷新,改完免重启。
     权益表无会员/商品快照列:会员名/手机联 user_info、商品名联 pt_product(当前名,商品改名后按新名展示)、
     来源订单号联 pt_private_order;时间列一律 DATE_FORMAT 格式化(惯例见 8c2b238)。 -->
<mapper namespace="com.dlc.modules.sys.dao.SysMemberBenefitDao">

    <!-- 公共筛选(不含 status,供列表与统计卡共用;统计卡按状态 CASE 分桶不吃 status 过滤) -->
    <sql id="CommonFilter">
        <if test="memberKeyword != null and memberKeyword != ''">
            AND (u.nickname LIKE concat('%', #{memberKeyword}, '%')
                 OR u.phone LIKE concat('%', #{memberKeyword}, '%'))
        </if>
        <if test="productKeyword != null and productKeyword != ''">
            AND p.product_name LIKE concat('%', #{productKeyword}, '%')
        </if>
        <if test="storeId != null and storeId != ''">
            AND b.store_id = #{storeId}
        </if>
        <!-- 生效时间区间;止日含当天(&lt;次日零点) -->
        <if test="startTime != null and startTime != ''">
            AND b.created_at &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            AND b.created_at &lt; DATE_ADD(#{endTime}, INTERVAL 1 DAY)
        </if>
        <!-- 门店数据权限:storeIds 为逗号分隔串,空则不过滤(超级管理员看全部) -->
        <if test="storeIds != null and storeIds != ''">
            AND b.store_id IN (${storeIds})
        </if>
    </sql>

    <sql id="Joins">
        FROM pt_member_private_benefit b
        LEFT JOIN pt_private_order o ON o.id = b.order_id
        LEFT JOIN user_info u ON u.userId = b.member_id
        LEFT JOIN pt_product p ON p.id = b.product_id
        LEFT JOIN store_address sa ON sa.storeAddrId = b.store_id
        LEFT JOIN store s ON s.storeId = sa.storeId
        LEFT JOIN pt_coach c ON c.id = b.coach_id
    </sql>

    <sql id="Columns">
        b.id,
        b.benefit_no          AS benefitNo,
        b.order_id            AS orderId,
        o.order_no            AS orderNo,
        b.member_id           AS memberId,
        u.nickname            AS memberName,
        u.phone               AS memberMobile,
        b.product_id          AS productId,
        p.product_name        AS productName,
        b.store_id            AS storeId,
        s.storeName           AS storeName,
        b.coach_id            AS coachId,
        c.coach_name          AS coachName,
        b.total_lessons       AS totalLessons,
        b.used_lessons        AS usedLessons,
        b.frozen_lessons      AS frozenLessons,
        b.remaining_lessons   AS remainingLessons,
        b.status              AS status,
        DATE_FORMAT(b.effective_at, '%Y-%m-%d %H:%i:%s') AS effectiveAt,
        DATE_FORMAT(b.expire_at, '%Y-%m-%d %H:%i:%s')    AS expireAt,
        DATE_FORMAT(b.created_at, '%Y-%m-%d %H:%i:%s')   AS createdAt
    </sql>

    <select id="queryList" resultType="java.util.LinkedHashMap" parameterType="map">
        SELECT <include refid="Columns"/>
        <include refid="Joins"/>
        <where>
            1 = 1
            <if test="status != null and status != ''">
                AND b.status = #{status}
            </if>
            <include refid="CommonFilter"/>
        </where>
        ORDER BY b.created_at DESC, b.id DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int" parameterType="map">
        SELECT COUNT(1)
        <include refid="Joins"/>
        <where>
            1 = 1
            <if test="status != null and status != ''">
                AND b.status = #{status}
            </if>
            <include refid="CommonFilter"/>
        </where>
    </select>

    <!-- 顶部统计卡:各状态数量 + 剩余/冻结课时合计,一条聚合 SQL 出全部指标;
         沿用除 status 外的筛选(状态由 CASE 分桶) -->
    <select id="queryStat" resultType="java.util.LinkedHashMap" parameterType="map">
        SELECT
            COUNT(CASE WHEN b.status = 1 THEN 1 END)          AS activeCount,
            COUNT(CASE WHEN b.status = 2 THEN 1 END)          AS usedUpCount,
            COUNT(CASE WHEN b.status = 3 THEN 1 END)          AS expiredCount,
            COUNT(CASE WHEN b.status = 4 THEN 1 END)          AS refundedCount,
            IFNULL(SUM(CASE WHEN b.status = 1 THEN b.remaining_lessons ELSE 0 END), 0) AS totalRemainingLessons,
            IFNULL(SUM(CASE WHEN b.status = 1 THEN b.frozen_lessons ELSE 0 END), 0)    AS totalFrozenLessons
        <include refid="Joins"/>
        <where>
            1 = 1
            <include refid="CommonFilter"/>
        </where>
    </select>

    <!-- 服务人候选:只返回启用教练,并按教练-门店关系复用当前管理员门店隔离。 -->
    <select id="queryCoachOptions" resultType="java.util.LinkedHashMap">
        SELECT
            c.id,
            c.coach_name AS coachName,
            c.mobile,
            (SELECT GROUP_CONCAT(s2.storeName ORDER BY r2.store_id)
               FROM pt_coach_store_rel r2
               LEFT JOIN store_address sa2 ON sa2.storeAddrId = r2.store_id
               LEFT JOIN store s2 ON s2.storeId = sa2.storeId
              WHERE r2.coach_id = c.id) AS storeNames
        FROM pt_coach c
        WHERE c.deleted = 0
          AND c.status = 1
          AND (CAST(c.id AS CHAR) LIKE concat('%', #{keyword}, '%')
               OR c.coach_name LIKE concat('%', #{keyword}, '%')
               OR c.mobile LIKE concat('%', #{keyword}, '%'))
        <if test="storeIds != null and storeIds != ''">
            AND EXISTS (SELECT 1 FROM pt_coach_store_rel r
                        WHERE r.coach_id = c.id AND r.store_id IN (${storeIds}))
        </if>
        ORDER BY c.sort_no DESC, c.id DESC
        LIMIT 20
    </select>

    <!-- 详情:storeIds 过滤在 SQL 内收口,越权/不存在返回 null(controller 按 404 处理) -->
    <select id="queryDetail" resultType="java.util.LinkedHashMap">
        SELECT <include refid="Columns"/>
        <include refid="Joins"/>
        WHERE b.id = #{id}
        <if test="storeIds != null and storeIds != ''">
            AND b.store_id IN (${storeIds})
        </if>
        LIMIT 1
    </select>

    <!-- 批量改到期日的前置校验:长期有效记录没有可偏移的 expire_at,退款记录不可改。 -->
    <select id="countAdjustableBenefits" resultType="int">
        SELECT COUNT(1)
        FROM pt_member_private_benefit b
        WHERE b.id IN
        <foreach collection="benefitIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND b.status IN (1, 2, 3)
          AND b.expire_at IS NOT NULL
        <if test="storeAddrIds != null and storeAddrIds != ''">
            AND b.store_id IN (${storeAddrIds})
        </if>
    </select>

    <!-- 每条记录都以自己的原 expire_at 为基准调整,不把批量记录改成同一个绝对日期。 -->
    <update id="updateExpireDateBatch">
        UPDATE pt_member_private_benefit
        SET expire_at = DATE_ADD(expire_at, INTERVAL #{offsetDays} DAY),
            status = CASE
                WHEN status = 1
                     AND DATE_ADD(expire_at, INTERVAL #{offsetDays} DAY) &lt;= NOW()
                     AND frozen_lessons = 0 THEN 3
                WHEN status = 3
                     AND DATE_ADD(expire_at, INTERVAL #{offsetDays} DAY) &gt; NOW()
                     AND remaining_lessons &gt; 0 THEN 1
                ELSE status
            END
        WHERE id IN
        <foreach collection="benefitIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND status IN (1, 2, 3)
          AND expire_at IS NOT NULL
        <if test="storeAddrIds != null and storeAddrIds != ''">
            AND store_id IN (${storeAddrIds})
        </if>
    </update>

    <!-- 批量变更门店的前置校验:退款权益不能再调整未来归属门店。 -->
    <select id="countChangeableBenefits" resultType="int">
        SELECT COUNT(1)
        FROM pt_member_private_benefit b
        WHERE b.id IN
        <foreach collection="benefitIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND b.status IN (1, 2, 3)
        <if test="storeAddrIds != null and storeAddrIds != ''">
            AND b.store_id IN (${storeAddrIds})
        </if>
    </select>

    <!-- 目标门店按 store_address.storeAddrId 校验,权限字段与权益表 store_id 同口径。 -->
    <select id="countStoreAddressInScope" resultType="int">
        SELECT COUNT(1)
        FROM store_address sa
        INNER JOIN store s ON s.storeId = sa.storeId
        WHERE sa.storeAddrId = #{storeAddrId}
          AND sa.status = 1
          AND s.status = 1
        <if test="storeAddrIds != null and storeAddrIds != ''">
            AND sa.storeAddrId IN (${storeAddrIds})
        </if>
    </select>

    <!-- 只改权益未来使用门店;预约表已有 store_id 不在本动作中修改,保留历史事实。 -->
    <update id="updateStoreBatch">
        UPDATE pt_member_private_benefit b
        INNER JOIN store_address target_sa
            ON target_sa.storeAddrId = #{targetStoreAddrId}
           AND target_sa.status = 1
        INNER JOIN store target_s
            ON target_s.storeId = target_sa.storeId
           AND target_s.status = 1
        SET b.store_id = target_sa.storeAddrId
        WHERE b.id IN
        <foreach collection="benefitIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND b.status IN (1, 2, 3)
        <if test="storeAddrIds != null and storeAddrIds != ''">
            AND b.store_id IN (${storeAddrIds})
            AND target_sa.storeAddrId IN (${storeAddrIds})
        </if>
    </update>

    <!-- 目标服务人必须有效,且与每条权益当前门店存在教练-门店关系。 -->
    <select id="countCoachChangeableBenefits" resultType="int">
        SELECT COUNT(DISTINCT b.id)
        FROM pt_member_private_benefit b
        INNER JOIN pt_coach c
            ON c.id = #{coachId}
           AND c.deleted = 0
           AND c.status = 1
        INNER JOIN pt_coach_store_rel csr
            ON csr.coach_id = c.id
           AND csr.store_id = b.store_id
        WHERE b.id IN
        <foreach collection="benefitIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND b.status IN (1, 2, 3)
        <if test="storeAddrIds != null and storeAddrIds != ''">
            AND b.store_id IN (${storeAddrIds})
        </if>
    </select>

    <!-- 只修改权益当前所属服务人;空 coachId 用于明确清空服务人。 -->
    <update id="updateCoachBatch">
        UPDATE pt_member_private_benefit
        SET coach_id = #{targetCoachId}
        WHERE id IN
        <foreach collection="benefitIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND status IN (1, 2, 3)
        <if test="storeAddrIds != null and storeAddrIds != ''">
            AND store_id IN (${storeAddrIds})
        </if>
    </update>

</mapper>
