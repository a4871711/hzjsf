<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlc.modules.sys.dao.PtProductCoachRelDao">

    <insert id="save" parameterType="com.dlc.modules.sys.entity.PtProductCoachRelEntity" useGeneratedKeys="true" keyProperty="id">
        insert into pt_product_coach_rel (`product_id`, `coach_id`, `created_at`)
        values (#{productId}, #{coachId}, #{createdAt})
    </insert>

    <delete id="deleteByProductId">
        delete from pt_product_coach_rel where product_id = #{value}
    </delete>

    <select id="queryCoachIds" resultType="java.lang.Long">
        select coach_id from pt_product_coach_rel where product_id = #{value}
    </select>

</mapper>
