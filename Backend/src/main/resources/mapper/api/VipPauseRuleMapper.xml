<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dlc.modules.api.dao.VipPauseRuleMapper">

    <!-- 按主键查停卡规则(含 tiers_json/status,供停卡预检/付费申请解析档位) -->
    <select id="selectById" resultType="com.dlc.modules.api.entity.VipPauseRule" parameterType="long">
        SELECT pause_rule_id AS pauseRuleId, rule_name AS ruleName, tiers_json AS tiersJson,
               status, created_date AS createdDate
        FROM vip_pause_rule
        WHERE pause_rule_id = #{pauseRuleId}
    </select>
</mapper>
