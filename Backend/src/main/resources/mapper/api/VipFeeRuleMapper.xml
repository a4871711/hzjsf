<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dlc.modules.api.dao.VipFeeRuleMapper">

    <!-- 按主键查费用规则(含 tiers_json/status,供 calcTransferFee 解析分档) -->
    <select id="selectById" resultType="com.dlc.modules.api.entity.VipFeeRule" parameterType="long">
        SELECT fee_rule_id AS feeRuleId, rule_name AS ruleName, tiers_json AS tiersJson,
               status, created_date AS createdDate
        FROM vip_fee_rule
        WHERE fee_rule_id = #{feeRuleId}
    </select>
</mapper>
